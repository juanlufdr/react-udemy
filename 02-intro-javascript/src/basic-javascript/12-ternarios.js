const active = true;

// let msg = ``;

// if (active) {
//   msg = 'Activo';
// } else {
//   msg = 'Inactivo';
// }

// const msg = active ? 'Activo' : 'Inactivo';
// const msg = active ? 'Activo' : null;
const msg = active && 'Activo';

console.log(msg);
